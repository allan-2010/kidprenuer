<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Kidpreneur Ideas</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      margin: 0;
      padding: 20px;
      background: #f4f7fb;
    }
    header {
      background: #0077ff;
      color: white;
      padding: 15px;
      text-align: center;
      border-radius: 10px;
    }
    .idea-form, .ideas-list {
      background: white;
      padding: 20px;
      margin: 20px 0;
      border-radius: 10px;
      box-shadow: 0 4px 8px rgba(0,0,0,0.1);
    }
    input, textarea, button {
      width: 100%;
      padding: 10px;
      margin: 10px 0;
      border-radius: 5px;
      border: 1px solid #ddd;
    }
    button {
      background: #0077ff;
      color: white;
      border: none;
      cursor: pointer;
    }
    button:hover {
      background: #005fcc;
    }
    .idea {
      border-bottom: 1px solid #ddd;
      padding: 10px 0;
    }
  </style>
</head>
<body>

  <header>
    <h1>Kidpreneur - Startup Ideas</h1>
    <p>Submit and explore creative startup ideas by students</p>
  </header>

  <section class="idea-form">
    <h2>Submit Your Idea</h2>
    <form id="ideaForm">
      <input type="text" id="title" placeholder="Startup Idea Title" required>
      <textarea id="problem" placeholder="What problem does your idea solve?" required></textarea>
      <button type="submit">Submit Idea</button>
    </form>
  </section>

  <section class="ideas-list">
    <h2>All Startup Ideas</h2>
    <div id="ideasContainer"></div>
  </section>

  <script>
    const form = document.getElementById('ideaForm');
    const ideasContainer = document.getElementById('ideasContainer');

    form.addEventListener('submit', function(e) {
      e.preventDefault();

      const title = document.getElementById('title').value;
      const problem = document.getElementById('problem').value;

      const ideaDiv = document.createElement('div');
      ideaDiv.classList.add('idea');
      ideaDiv.innerHTML = `<h3>${title}</h3><p>${problem}</p>`;

      ideasContainer.appendChild(ideaDiv);

      form.reset();
    });
  </script>

</body>
</html>
